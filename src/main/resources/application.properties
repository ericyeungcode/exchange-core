# ============================================================================
# Exchange Core REST API Configuration
# ============================================================================

# Server Configuration
server.port=8080
server.shutdown=graceful
spring.application.name=exchange-core-api

# Logging Configuration
logging.level.root=INFO
logging.level.exchange.core2=DEBUG
logging.level.org.springframework.web=INFO

# Logging pattern
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n

# Jackson JSON Configuration
spring.jackson.serialization.indent-output=true
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.default-property-inclusion=non_null

# Actuator Configuration (if enabled)
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized

# ============================================================================
# Exchange Core Specific Configuration (Future Enhancement)
# ============================================================================
# These are placeholder properties for future customization
# Currently, ExchangeCore uses default configuration

# exchange.core.ring-buffer-size=8192
# exchange.core.matching-engines-num=1
# exchange.core.risk-engines-num=1
# exchange.core.wait-strategy=BUSY_SPIN
# exchange.core.journaling.enabled=false
# exchange.core.journaling.path=./data/journal
